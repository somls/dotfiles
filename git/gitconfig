# Git 全局配置文件 - dotfiles 统一管理
# 此文件作为系统全局 ~/.gitconfig 使用，通过符号链接连接

[include]
    # 核心设置
    path = ~/.gitconfig.d/core.gitconfig
    # 命令别名
    path = ~/.gitconfig.d/aliases.gitconfig
    # 颜色配置
    path = ~/.gitconfig.d/color.gitconfig
    # 差异比较设置
    path = ~/.gitconfig.d/diff.gitconfig
    # Windows 特定设置
    path = ~/.gitconfig.d/windows.gitconfig

[include]
    # 本地配置文件（用户信息、代理设置等）
    # 包含个人信息和环境特定设置
    path = ~/.gitconfig.local

# 默认的提交消息模板
[commit]
    template = ~/.gitmessage

# 默认分支名称
[init]
    defaultBranch = main

# 安全设置
[safe]
    # 信任 dotfiles 目录
    directory = G:/Sync/dotfiles

# 代理配置说明：
# 如需配置代理，请在 .gitconfig.local 文件中添加：
# [http]
#     proxy = http://127.0.0.1:PORT
# [https]
#     proxy = http://127.0.0.1:PORT
#
# 这样做的好处：
# 1. 避免在版本控制中包含代理设置
# 2. 不同环境可以有不同的代理配置
# 3. 保持主配置文件的清洁性
