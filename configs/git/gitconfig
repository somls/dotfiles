[user]
 name = Default User
 email = user@example.com
[core]
 editor = code --wait
[init]
 defaultBranch = main
[pull]
 rebase = true
[color]
 ui = auto
# Note: Dynamic safe directories should be configured locally
# Use: git config --global --add safe.directory "/path/to/scoop"
