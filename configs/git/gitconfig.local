# 本地 Git 配置 - 不会被版本控制跟踪
# 此文件用于存储特定于此机器的 Git 配置

[user]
	name = yadansu
	email = plyuan@outlook.com
    # 覆盖全局用户设置（如果需要）
    # name = Your Name
    # email = your.email@example.com
    # signingkey = YOUR_GPG_KEY_ID

[github]
    # GitHub 特定配置
    # user = your-github-username

[credential]
    # 凭据助手配置
    # helper = wincred
    # helper = manager

# 其他特定于此机器的设置
