# Local git user configs (kept in-repo but not tracked)
git/gitconfig.local
git/.gitconfig.local
git/.gitconfig.work
git/.gitconfig.private

# Allow important git documentation
!git/USAGE.md

# Temporary files
*.tmp
*.log
*.user
*.password
*.bak
*~

# Environment files
.env
.env.local
.env.production

# PowerShell history
pshistory.txt

# Backup files
*_backup/
*-backup/
*.backup

# System files
Thumbs.db
.DS_Store
desktop.ini

# IDE/Editor files
.vscode/settings.json
.idea/
*.swp
*.swo

# Test artifacts
TestResults/
*.coverage
*.trx

# Build outputs
bin/
obj/
dist/
build/


# Project-specific local state (do not sync)
.kiro/

# Local credentials, histories, and reports (do not sync)

powershell/.powershell/pshistory.txt
health-report.json

# User-specific configuration files (contain personal info)
git/gitconfig.local
git/.gitconfig.*
# scoop/config.json (用户从 config.json.example 复制)


# Personal proxy and network settings
**/proxy.conf
**/*proxy*
**/network.conf

# Exception: Allow proxy documentation
!PROXY_CONFIG_UPDATE.md

# User-specific paths and drives
**/*G:/*
**/*C:/Users/*
**/*malip*
yncdotfiles'

# Installation logs and temporary files
install.log
*.backup
backup/
*_backup/

# Auto-sync script (was ignored previously)
# auto-sync.ps1

# Development documentation (local only)
DEVELOPMENT.md
CLAUDE.md
**/PERSONAL_NOTES.md

# Scripts guide (local only, optional)
SCRIPTS_GUIDE.md

# Privacy audit report (local only)
PRIVACY_AUDIT_REPORT.md

# Git history cleaning script (local only)
clean-git-history.ps1

# Git sensitive configuration and security files
**/.git-credentials
**/.gitconfig.d/sensitive.gitconfig
**/.gitconfig.d/*_local.gitconfig
**/.gitconfig.d/*_private.gitconfig
